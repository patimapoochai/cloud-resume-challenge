# this shall be the file where i run this to manually setup necessary resources on
## a fresh aws account

# open id connect provider
## if there is already a provider named X, don't deploy this provider
## instead reference the provider as data

# remote state for infrastructure

# github actions role
